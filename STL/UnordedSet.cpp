#include<iostream>
#include<unordered_set>
using namespace std;

void explainUSet() {
    unordered_set<int> st;
    //lower_bound and upper_bound function does not work, rest all functions are same. it does not stores any particular order it has a better complexity than set in most cases, except some when collision happens
}